import SignInClient from './signinclient';

export default function LoginPage() {
    return (
        <div className="flex border justify-center h-[100vh] items-center bg-gray-400">
            <SignInClient />
        </div>
    );
}
